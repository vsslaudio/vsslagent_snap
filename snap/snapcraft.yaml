name: vsslagent
version: '1.1' 
summary: VSSL bling
description: VSSL remote control features.  REST api.  Configuration assistance.
grade: stable
confinement: strict
apps:
   agent:
      command: main-linux
      daemon: simple
      restart-condition: always 
      plugs: [network]
parts:
  vssl:
    source: .
    plugin: dump
