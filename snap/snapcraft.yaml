name: vsslagent
version: '1.0' 
summary: VSSL bling
description: VSSL remote control features.  REST api.  Configuration assistance.
grade: stable
confinement: devmode
apps:
   agent:
      command: main-linux
      daemon: simple
      restart-condition: always 
      plugs: [network]
parts:
  vssl:
    source: .
    plugin: dump
